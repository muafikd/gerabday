# 💛 Мини-игры

Главная страница-лобби с вкладками и мини-играми в кавайном стиле. Одна вкладка — одна игра; внизу — кнопка «Открыть сюрприз» с финальным сообщением.

## Деплой на GitHub Pages

1. Создай репозиторий на GitHub.
2. Загрузи `index.html`.
3. **Settings → Pages** → Source: **Deploy from a branch** → ветка `main`, папка **root**.
4. Сайт: `https://<логин>.github.io/<репо>/`.

## Что внутри

- **Вкладки (лобби):** Memory, Сердечки, Пазл — переключение без перезагрузки.
- **Memory** — найди пары карточек с эмодзи (8 пар).
- **Сердечки** — нажимай «Старт», лови появляющиеся сердечки 15 секунд.
- **Пазл** — собери картинку из 6 частей (2×3): перетаскивай части на поле; после сборки показывается мини-сюрприз (текст и при желании фото).
- **Сюрприз** — кнопка «Открыть сюрприз» открывает карточку с текстом (его можно заменить в HTML).

## Пазл: как вставить своё фото

В `index.html` в начале скрипта пазла найди константы:

```js
const PUZZLE_IMAGE = '';           // URL фотографии для пазла (например 'puzzle.jpg')
const PUZZLE_SURPRISE_TEXT = '...'; // Текст после сборки
const PUZZLE_SURPRISE_IMAGE = '';   // Опционально: URL фото в блоке сюрприза
```

- **PUZZLE_IMAGE** — одна картинка; она автоматически режется на 6 частей (2 ряда по 3). Подойдёт любое фото (портрет, пейзаж). Если оставить `''`, вместо фото будут заглушки с эмодзи.
- **PUZZLE_SURPRISE_IMAGE** — если указать URL, под текстом сюрприза покажется ещё одно фото.

## Как добавить новую мини-игру

1. **Вкладка.** В блоке `<nav class="tabs">` добавь кнопку:
   ```html
   <button type="button" class="tab" data-tab="mygame">🦋 Моя игра</button>
   ```

2. **Блок игры.** После других `<section class="game-block">` добавь:
   ```html
   <section class="game-block" id="game-mygame">
     <div class="game-card">
       <h2 class="game-title">🦋 Моя игра</h2>
       <!-- контент и кнопки -->
     </div>
   </section>
   ```

3. **Логика.** Переключение вкладок уже привязано к `data-tab` и `id="game-<tab>"`, поэтому новая вкладка автоматически будет показывать свой блок. Остаётся добавить свой JS для правил игры.

## Как изменить текст сюрприза

В `index.html` найди блок с `id="surprise-card"` и отредактируй абзац внутри, либо в конце скрипта задай:
```js
surpriseText.textContent = 'Твой текст...';
```

Стили и скрипты находятся в том же файле, сервер не нужен.
# gerabday
